{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "a",
            "database": "${workspaceFolder:pet_shop}/db.sqlite"
        }
    ]
}