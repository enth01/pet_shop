This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
eslint.config.mjs
kysely.config.ts
migrations/1762706948911_create_ products_and_users.ts
migrations/1763388106447_create_session.ts
migrations/1764093601083_add_admin_col.ts
migrations/1765999330032_create_order.ts
migrations/1766001491719_create_order_items.ts
migrations/1768321026001_add_user_address.ts
migrations/1768326003783_update_user_address.ts
next-env.d.ts
next.config.ts
package.json
public/banner_images/banner1.png
public/banner_images/banner2.jpeg
public/banner_images/banner3.jpg
public/uploads/advantix_pipety_pre_psy.webp
public/uploads/animonda_carny_adult_hovädzie.webp
public/uploads/beaphar_care+_pre_potkany.webp
public/uploads/brit_animals_rabbit_junior.webp
public/uploads/brit_care_cat_grain-free_sterilized.webp
public/uploads/brit_care_dog_show_champion.webp
public/uploads/brit_training_snacks_l.webp
public/uploads/catit_fontána_na_vodu_flower.webp
public/uploads/chuckit_ultra_ball.webp
public/uploads/cosma_nature_tuniak.webp
public/uploads/dennerle_nano_deponitmix.webp
public/uploads/eheim_vnútorný_filter_pickup.webp
public/uploads/ever_clean_litter_podstielka.webp
public/uploads/felix_fantastic_s_kuracím.webp
public/uploads/ferplast_klietka_pre_kanáriky.webp
public/uploads/flexi_new_classic_vodítko.webp
public/uploads/fluval_led_osvetlenie.webp
public/uploads/gimcat_malt-soft_pasta_professional.webp
public/uploads/hill's_science_plan_adult_light.webp
public/uploads/jbl_pronovo_bel_flakes.webp
public/uploads/jr_farm_klasy_senegalského_prosa.webp
public/uploads/julius-k9_idc_postroj.webp
public/uploads/juvel_bioflow_filtračná_vata.webp
public/uploads/kong_classic_gumená_hračka.webp
public/uploads/pedigree_dentastix_medium.webp
public/uploads/pedigree_junior_kapsička_hovädzia.webp
public/uploads/purina_one_sterilcat.webp
public/uploads/purina_pro_plan_small_and_mini_adult.webp
public/uploads/royal_canin_kitten.webp
public/uploads/royal_canin_medium_adult.webp
public/uploads/royal_canin_urinary_so_pre_mačky.webp
public/uploads/savic_klietka_pre_škrečka.webp
public/uploads/savic_záchod_pre_mačky_nestor.webp
public/uploads/sera_aquatan_kondicionér.webp
public/uploads/sheba_selection_v_omáčke.webp
public/uploads/tetra_weekend_krmivo.webp
public/uploads/tetramin_vločkové_krmivo.webp
public/uploads/trixie_chladniaca_podložka.webp
public/uploads/trixie_drevený_domček.webp
public/uploads/trixie_kúpeľnička_pre_vtáky.webp
public/uploads/trixie_plyšový_macko.webp
public/uploads/trixie_škrabadlo_zaragoza.webp
public/uploads/trixie_zubná_pasta_pre_psy.webp
public/uploads/versele-laga_cuni_complete.webp
public/uploads/versele-laga_prestige_loro_parque.webp
public/uploads/vitakraft_kräcker_tyčinky.webp
public/uploads/vitakraft_menu_pre_andulky.webp
public/uploads/whiskas_kapsičky_klasický_výber.webp
seeds/1765049644188_new_seed.ts
src/actions/admin.ts
src/actions/products.ts
src/actions/user.ts
src/app/admin/AddProductForm.tsx
src/app/admin/AdminDashboard.tsx
src/app/admin/OrdersManager.tsx
src/app/admin/page.tsx
src/app/admin/ProductEditor.tsx
src/app/admin/UserManager.tsx
src/app/basket/page.tsx
src/app/basket/removeFromCartButton.tsx
src/app/carousel/HeroCarousel.module.css
src/app/carousel/HeroCarousel.tsx
src/app/confirmation/orderButton.tsx
src/app/confirmation/page.tsx
src/app/confirmed/page.tsx
src/app/favicon.ico
src/app/footer.module.css
src/app/footer.tsx
src/app/form.module.css
src/app/globals.css
src/app/layout.tsx
src/app/login/loginButton.tsx
src/app/login/page.tsx
src/app/navbar.module.css
src/app/navbar.tsx
src/app/page.module.css
src/app/page.tsx
src/app/password_change/page.tsx
src/app/password_change2/page.tsx
src/app/password_change2/verifyButton.tsx
src/app/password_change3/page.tsx
src/app/password_change4/page.tsx
src/app/products/addToCartButton.tsx
src/app/products/addToFavouritesButton.tsx
src/app/products/page.tsx
src/app/products/products.module.css
src/app/products/ProductsClient.tsx
src/app/products/removeFromFavouritesButton.tsx
src/app/products/singleProduct.tsx
src/app/register/page.tsx
src/app/register/registerButton.tsx
src/app/shipping_info/LinkToConfirmation.tsx
src/app/shipping_info/page.tsx
src/app/user/address/page.tsx
src/app/user/email/page.tsx
src/app/user/logoutButton.tsx
src/app/user/page.module.css
src/app/user/page.tsx
src/lib/db-types.ts
src/lib/db.ts
src/lib/hashing.ts
src/lib/types.ts
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="eslint.config.mjs">
import { dirname } from "path";
import { fileURLToPath } from "url";
import { FlatCompat } from "@eslint/eslintrc";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

const compat = new FlatCompat({
  baseDirectory: __dirname,
});

const eslintConfig = [
  ...compat.extends("next/core-web-vitals", "next/typescript"),
  {
    ignores: [
      "node_modules/**",
      ".next/**",
      "out/**",
      "build/**",
      "next-env.d.ts",
    ],
  },
];

export default eslintConfig;
</file>

<file path="kysely.config.ts">
import SQLite from "better-sqlite3";
import { SqliteDialect } from "kysely";
import { defineConfig } from "kysely-ctl";

export default defineConfig({
  dialect: new SqliteDialect({
    database: new SQLite("db.sqlite"),
  }),
});
</file>

<file path="next-env.d.ts">
/// <reference types="next" />
/// <reference types="next/image-types/global" />
/// <reference path="./.next/types/routes.d.ts" />

// NOTE: This file should not be edited
// see https://nextjs.org/docs/app/api-reference/config/typescript for more information.
</file>

<file path="next.config.ts">
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactStrictMode: false,
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
      {
        protocol: 'http',
        hostname: '**',
      },
    ],
  },
};

export default nextConfig;
</file>

<file path="package.json">
{
  "name": "pet_shop",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build --turbopack",
    "start": "next start",
    "db:migrate:make": "kysely migrate:make new_migration",
    "db:migrate:latest": "kysely migrate:latest",
    "db:codegen": "kysely-codegen --url db.sqlite --out-file src/lib/db-types.ts",
    "db:seed:make": "kysely seed:make new_seed",
    "db:seed:run": "kysely seed:run"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "better-sqlite3": "^12.2.0",
    "kysely": "^0.28.5",
    "next": "^15.5.7",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "resend": "^6.6.0",
    "validator": "^13.15.23"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/bcrypt": "^6.0.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@types/validator": "^13.15.10",
    "eslint": "^9",
    "eslint-config-next": "15.5.2",
    "kysely-codegen": "^0.19.0",
    "kysely-ctl": "^0.19.0",
    "tsx": "^4.20.5",
    "typescript": "^5"
  },
  "overrides": {
    "kysely-codegen": {
      "better-sqlite3": "^12.2.0"
    }
  }
}
</file>

<file path="src/app/admin/AdminDashboard.tsx">
"use client";

import { useState } from "react";
import type { Products, Users, Orders } from "@/lib/db-types";
import AddProductForm from "./AddProductForm";
import ProductEditor from "./ProductEditor";
import UserManager from "./UserManager";
import OrdersManager from "./OrdersManager";
import type { Selectable } from "kysely";

export default function AdminDashboard(props: {
  products: Selectable<Products>[];
  users: Pick<Selectable<Users>, "id" | "username" | "email" | "is_admin">[];
  orders: Selectable<Orders>[];
}) {
  const [tab, setTab] = useState<"products" | "users" | "orders">("products");

  return (
    <main className="admin">
      <h1>Admin panel</h1>

      <nav className="admin-tabs">
        <button onClick={() => setTab("products")}>Products</button>
        <button onClick={() => setTab("users")}>Users</button>
        <button onClick={() => setTab("orders")}>Orders</button>
      </nav>

      {tab === "products" && (
        <>
          <AddProductForm />
          <ProductEditor products={props.products} />
        </>
      )}

      {tab === "users" && <UserManager users={props.users} />}

      {tab === "orders" && <OrdersManager orders={props.orders} />}
    </main>
  );
}
</file>

<file path="src/app/admin/OrdersManager.tsx">
"use client";

import type { Orders } from "@/lib/db-types";
import { deleteOrder, markOrderAsSent } from "@/actions/admin";
import { Selectable } from "kysely";

export default function OrdersManager({
  orders,
}: {
  orders: Selectable<Orders>[];
}) {
  return (
    <section className="admin-box">
      <h2>Orders</h2>

      {orders.map(o => (
        <div key={o.id} className="admin-row">
          <span>
            {o.email} | {o.city}
          </span>

          {o.confirmed === 0 && (
            <button onClick={() => markOrderAsSent(o.id, o.email)}>
              Mark as sent
            </button>
          )}

          <button onClick={() => deleteOrder(o.id)}>Delete</button>
        </div>
      ))}
    </section>
  );
}
</file>

<file path="src/app/admin/page.tsx">
import getDB from "@/lib/db";
import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import AdminDashboard from "./AdminDashboard";

export default async function AdminPage() {
  const cookieStore = await cookies();
  const sessionId = cookieStore.get("session")?.value;
  if (!sessionId) redirect("/");

  const db = getDB();

  const session = await db
    .selectFrom("session")
    .selectAll()
    .where("session_id", "=", sessionId)
    .executeTakeFirst();

  if (!session) redirect("/");

  const admin = await db
    .selectFrom("users")
    .selectAll()
    .where("id", "=", session.user_id)
    .executeTakeFirst();

  if (!admin || admin.is_admin === 0) redirect("/");

  const products = await db.selectFrom("products").selectAll().execute();
  const users = await db
    .selectFrom("users")
    .select(["id", "username", "email", "is_admin"])
    .execute();
  const orders = await db.selectFrom("orders").selectAll().execute();

  return (
    <AdminDashboard
      products={products}
      users={users}
      orders={orders}
    />
  );
}
</file>

<file path="src/app/admin/UserManager.tsx">
"use client";

import { deleteUserAndData } from "@/actions/admin";

export default function UserManager(props: {
  users: { id: number; username: string; email: string; is_admin: number | null }[];
}) {
  return (
    <section className="admin-box">
      <h2>Users</h2>

      {props.users.map(u => (
        <div key={u.id} className="admin-row">
          <span>{u.username} ({u.email})</span>
          {u.is_admin ? (
            <span>Admin</span>
          ) : (
            <button onClick={() => deleteUserAndData(u.id)}>Delete</button>
          )}
        </div>
      ))}
    </section>
  );
}
</file>

<file path="src/app/basket/removeFromCartButton.tsx">
// basket/removeFromCartButton.tsx
"use client";

import { removeFromBasket } from "@/actions/products";

export default function RemoveFromCartButton({ id }: { id: number }) {
  return (
    <button
      onClick={() => removeFromBasket(id)}
      className="btn"
      style={{
        backgroundColor: "#4CAF50",
        color: "white",
        padding: "6px 12px",
        border: "none",
        borderRadius: 8,
        cursor: "pointer",
      }}
    >
      Remove
    </button>
  );
}
</file>

<file path="src/app/carousel/HeroCarousel.module.css">
.carousel {
    width: 100%;
    height: 360px;
    overflow: hidden;
    border-radius: 0.75rem;
    margin-bottom: 3rem;
  }
  
  .slider {
    display: flex;
    height: 100%;
    transition: transform 0.6s ease;
  }
  
  .slide {
    min-width: 100%;
    position: relative;
  }
  
  .slide img {
    object-fit: cover;
  }
  
  .overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.35);
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding-left: 4rem;
    color: white;
  }
  
  .overlay h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
  }
  
  .overlay p {
    font-size: 1.2rem;
  }
</file>

<file path="src/app/carousel/HeroCarousel.tsx">
"use client";

import Image from "next/image";
import { useEffect, useState } from "react";
import styles from "./HeroCarousel.module.css";

// each image now has its own overlay text
const slides = [
  {
    src: "/banner_images/banner1.png",
    title: "Všetko čo Váš miláčik potrebuje",
    subtitle: "Jedlo, hračky a iné doplnky",
  },
  {
    src: "/banner_images/banner2.jpeg",
    title: "Pohodlie ako nikde inde",
    subtitle: "Hebké postele, deky a viac",
  },
  {
    src: "/banner_images/banner3.jpg",
    title: "Zdravé maškrty, šťastné zvieratká",
    subtitle: "Mňamky a výživové doplnky z prírodných ingrediencií",
  },
];

export default function HeroCarousel() {
  const [index, setIndex] = useState(0);

  useEffect(() => {
    const interval = setInterval(() => {
      setIndex((prev) => (prev + 1) % slides.length);
    }, 4000);

    return () => clearInterval(interval);
  }, []);

  return (
    <div className={styles.carousel}>
      <div
        className={styles.slider}
        style={{ transform: `translateX(-${index * 100}%)` }}
      >
        {slides.map((slide, i) => (
          <div className={styles.slide} key={i}>
            <Image
              src={slide.src}
              alt={`Banner ${i + 1}`}
              fill
              priority={i === 0}
            />
            <div className={styles.overlay}>
              <h1>{slide.title}</h1>
              <p>{slide.subtitle}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
</file>

<file path="src/app/form.module.css">
/* login.module.css */
.container {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 4rem 2rem;
    margin-bottom: 5rem;
}

.title {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 2rem;
    color: #0f1f1a;
}

.form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    width: 100%;
    max-width: 400px;
    background: white;
    padding: 2.5rem;
    border-radius: 0.75rem;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
}

.field {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    font-weight: 500;
    text-transform: capitalize;
}

/* Leveraging the input style logic from your global/products theme */
.input {
    border-radius: 0.75rem;
    border: 1px solid #e5e7eb;
    padding: 0.75rem;
    font-size: 1rem;
    transition: border-color 0.2s ease;
}

.input:focus {
    outline: none;
    border-color: #22c55e;
}

.btn {
    padding: 0.75rem 1.5rem;
    border-radius: 0.75rem;
    border: 2px solid #22c55e;
    background: white;
    cursor: pointer;
    transition: background-color 0.2s ease, color 0.2s ease;
    text-transform: capitalize;
}

.btn:hover {
    background-color: #22c55e;
    color: white;
}

/* Add this to form.module.css */
.buttonContainer {
    display: flex;
    gap: 1rem;
    margin-top: 1rem;
}
</file>

<file path="src/app/globals.css">
body{
    font-family: Arial, Helvetica, sans-serif;
    background-color: #f0f0f0;
    margin: 0 1rem;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

.admin {
    padding: 2rem;
  }
  
  .admin-tabs button {
    margin-right: 1rem;
    border-radius: 0.75rem;
    background: white;
    border: 2px solid #2f9e44;
    padding: 0.5rem 1rem;
  }
  
  .admin-tabs button:hover {
    background: #2f9e44;
    color: white;
  }
  
  .admin-box {
    margin-top: 2rem;
    padding: 1.5rem;
    border-radius: 0.75rem;
    border: 2px solid #2f9e44;
  }
  
  .admin-row {
    display: flex;
    justify-content: space-between;
    margin: 0.5rem 0;
  }
  
  .card {
    background: #e5f6ef10; /* subtle greenish background */
    border-radius: 0.75rem;
    padding: 1.5rem;
    color: #0f1f1a;
  }
  
  .btn {
    background: #2ecc71;
    border-radius: 0.75rem;
    padding: 0.5rem 1rem;
    color: black;
    text-decoration: none;
    border: none;
    cursor: pointer;
  }
  
  .btn:hover {
    background: #27ae60;
  }
  
  input {
    border-radius: 0.75rem;
    border: 1px solid #2ecc71;
    padding: 0.5rem;
  }
</file>

<file path="src/app/layout.tsx">
import NavBar from "./navbar";
import "./globals.css";
import Footer from "./footer";

export default function RootLayout({children}: Readonly<{children: React.ReactNode;}>) {
  return (
    <html lang="en">
      <head>
      </head>
      <body>
        <NavBar />
        {children}
        <Footer />
      </body>
    </html>
  );
}
</file>

<file path="src/app/navbar.module.css">
.navbar {
    display: flex;
    padding: 1rem 2rem;
    align-items: center;
    justify-content: space-around;
    background-color: #ffffff;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.548);
    border-bottom-left-radius: 0.75rem;
    border-bottom-right-radius: 0.75rem;
  }
  
  .navItem {
    border-radius: 0.75rem;
  }
  
  .navLink {
    display: block;
    padding: 0.5rem 0.9rem;
    border-radius: 0.75rem;
    text-decoration: none;
    color: #1f2937; /* dark gray, not link-blue */
    font-weight: 500;
    transition: background-color 0.2s ease, color 0.2s ease;
  }
  
  .navLink:hover {
    background-color: #22c55e; /* green hover */
    color: #ffffff; /* explicitly not blue */
  }
</file>

<file path="src/app/page.module.css">
.home {
    padding: 2rem 3rem;
  }
  
  .latest h2 {
    font-size: 1.8rem;
    margin-bottom: 1.5rem;
  }
  
  .productRow {
    display: flex;
    justify-content: space-around;
    gap: 2rem;
  }
  
  .productCard {
    background: white;
    border-radius: 0.75rem;
    padding: 1.5rem;
    width: 220px;
    text-align: center;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
  }
  
  .productCard h3 {
    margin: 0.8rem 0 0.4rem;
  }
  
  .price {
    font-weight: 600;
    margin-bottom: 1rem;
  }
  
  .button {
    display: inline-block;
    padding: 0.5rem 1rem;
    border-radius: 0.75rem;
    text-decoration: none;
    color: #1f2937;
    background-color: #e5e7eb;
    transition: background-color 0.2s ease, color 0.2s ease;
  }
  
  .button:hover {
    background-color: #22c55e;
    color: white;
  }
</file>

<file path="src/app/password_change/page.tsx">
"use client"
import { passwordChange } from "@/actions/user";

export default function PasswordChangePage() {
  return (
    <div className="card max-w-md mx-auto flex flex-col gap-4">
      <h1>Change Password</h1>
      <p>We will send you a code to your email to verify.</p>
      <button className="btn" onClick={passwordChange}>Send code</button>
    </div>
  );
}
</file>

<file path="src/app/password_change2/verifyButton.tsx">
"use client"

import { verifyCode } from "@/actions/user";
import styles from "../form.module.css";

export default function VerifyButton(props: { code: string; }) {
    async function handleVerify() {
        const res = await verifyCode(props.code);
        if (res?.error) {
            alert(res.error);
        } else if (res?.success) {
            window.location.href = "/password_change3";
        }
    }
    return (
        <button onClick={handleVerify} className={styles.btn}>Verify</button>
    )
}
</file>

<file path="src/app/password_change4/page.tsx">
export default function PasswordChange4Page() {
    return (
      <div className="card max-w-md mx-auto flex flex-col gap-4 text-center">
        <h1>Password Changed!</h1>
        <p>Your new password has been set successfully.</p>
        <a href="/user" className="btn">Back to Account</a>
      </div>
    );
  }
</file>

<file path="src/app/products/addToCartButton.tsx">
"use client"

import { addToBasket } from "@/actions/products";

export default function AddToCartButton(props: { id: number }) {
    return <button className="btn" onClick={() => addToBasket(props.id)}>Add to cart</button>
}
</file>

<file path="src/app/products/addToFavouritesButton.tsx">
"use client"

import { addToFavourites } from "@/actions/products"

export default function AddToFavouritesButton(props: { id: number }) {
    return <button onClick={() => addToFavourites(props.id)}>Add to favourites</button>
}
</file>

<file path="src/app/products/page.tsx">
import getDB from "@/lib/db";
import { getUser } from "@/actions/user";
import { isInFavourites } from "@/actions/products";
import ProductsClient from "./ProductsClient";

export default async function ProductsPage() {
  const products = await getDB().selectFrom("products").selectAll().execute();
  const user = await getUser();

  const productsWithFav = [];

  for (const product of products) {
    const is_in_fav = await isInFavourites(product.id);
    productsWithFav.push({ ...product, is_in_fav });
  }

  // extract unique categories
  const categorySet = new Set<string>();

  products.forEach((product) => {
    product.category
      .split(",")
      .map((c) => c.trim())
      .forEach((c) => categorySet.add(c));
  });

  const categories = Array.from(categorySet);

  return (
    <ProductsClient
      products={productsWithFav}
      categories={categories}
      userLoggedIn={!!user}
    />
  );
}
</file>

<file path="src/app/products/removeFromFavouritesButton.tsx">
"use client"

import { removeFromFavourites } from "@/actions/products"

export default function RemoveFromFavouritesButton(props: { id: number }) {
    return <button onClick={() => removeFromFavourites(props.id)}>Remove from favourites</button>
}
</file>

<file path="src/app/products/singleProduct.tsx">
import styles from "./products.module.css";
import AddToCartButton from "./addToCartButton";
import AddToFavouritesButton from "./addToFavouritesButton";
import RemoveFromFavouritesButton from "./removeFromFavouritesButton";
import Image from "next/image";

interface ProductWithFav {
    id: number;
    name: string;
    description: string;
    image: string;
    price: number;
    category: string;
    is_in_fav: boolean;
}

export default function SingleProduct(props: { product: ProductWithFav; userLoggedIn: boolean }) {
    const { product, userLoggedIn } = props;
    return (
        <div className={styles.card} key={product.id}>
            <Image
                src={"/uploads/" + product.image}
                alt={product.name}
                width={200}
                height={200}
            />

            <h3>{product.name}</h3>
            <p className={styles.description}>{product.description}</p>

            <p className={styles.price}>{(product.price / 100).toFixed(2)} €</p>

            <div className={styles.actions}>
                <AddToCartButton id={product.id} />
                {userLoggedIn &&
                    (product.is_in_fav ? (
                        <RemoveFromFavouritesButton id={product.id} />
                    ) : (
                        <AddToFavouritesButton id={product.id} />
                    ))}
            </div>
        </div>
    );
}
</file>

<file path="src/app/user/address/page.tsx">
// user/address/page.tsx
import { getUser, updateAddress } from "@/actions/user";
import getDB from "@/lib/db";
import { redirect } from "next/navigation";

export default async function AddressPage() {
  const user = await getUser();
  if (!user) redirect("/");

  const db = getDB();

  const address = await db
    .selectFrom("user_address")
    .selectAll()
    .where("user_id", "=", user.id)
    .executeTakeFirst();

  return (
    <form
      action={updateAddress}
      style={{
        maxWidth: 500,
        padding: 24,
        display: "flex",
        flexDirection: "column",
        gap: 16,
      }}
    >
      <h1 style={{ fontSize: 24, fontWeight: 700 }}>Address</h1>

      <label>
        Street:
        <input
          name="street"
          defaultValue={address?.street ?? ""}
          required
          style={{ width: "100%", padding: 8, borderRadius: 8, marginTop: 4 }}
        />
      </label>

      <label>
        House number:
        <input
          name="house_number"
          defaultValue={address?.house_number ?? ""}
          required
          style={{ width: "100%", padding: 8, borderRadius: 8, marginTop: 4 }}
        />
      </label>

      <label>
        City:
        <input
          name="city"
          defaultValue={address?.city ?? ""}
          required
          style={{ width: "100%", padding: 8, borderRadius: 8, marginTop: 4 }}
        />
      </label>

      <label>
        Zip code:
        <input
          name="zip_code"
          defaultValue={address?.zip_code ?? ""}
          required
          style={{ width: "100%", padding: 8, borderRadius: 8, marginTop: 4 }}
        />
      </label>

      <label>
        Country:
        <input
          name="country"
          defaultValue={address?.country ?? ""}
          required
          style={{ width: "100%", padding: 8, borderRadius: 8, marginTop: 4 }}
        />
      </label>

      <label>
        Phone number:
        <input
          name="phone_number"
          defaultValue={address?.phone_number ?? ""}
          required
          style={{ width: "100%", padding: 8, borderRadius: 8, marginTop: 4 }}
        />
      </label>

      <label>
        Name:
        <input
          name="name"
          defaultValue={address?.name ?? ""}
          required
          style={{ width: "100%", padding: 8, borderRadius: 8, marginTop: 4 }}
        />
      </label>

      <label>
        Surname:
        <input
          name="surname"
          defaultValue={address?.surname ?? ""}
          required
          style={{ width: "100%", padding: 8, borderRadius: 8, marginTop: 4 }}
        />
      </label>

      <button
        type="submit"
        style={{
          backgroundColor: "#4CAF50",
          color: "white",
          padding: 12,
          border: "none",
          borderRadius: 12,
          fontWeight: 600,
          cursor: "pointer",
          marginTop: 12,
        }}
      >
        Save
      </button>
    </form>
  );
}
</file>

<file path="src/app/user/email/page.tsx">
// app/user/email/page.tsx
"use client"

import { useState } from "react";
import { updateEmail } from "@/actions/user";

export default function ChangeEmail() {
  const [email, setEmail] = useState("");

  return (
    <form
      className="card max-w-md mx-auto flex flex-col gap-4"
      onSubmit={async (e) => {
        e.preventDefault();
        const res = await updateEmail(email);
        if (res?.error) alert(res.error);
        else alert("Email updated!");
      }}
    >
      <h1>Change Email</h1>
      <input
        type="email"
        placeholder="New email"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        required
        className="border p-2 rounded"
      />
      <button type="submit" className="btn">Save</button>
    </form>
  );
}
</file>

<file path="src/app/user/logoutButton.tsx">
"use client"

import { logout } from "@/actions/user"

export default function LogoutButton() {
    return <button style={{float: "right", fontFamily: "Arial, Helvetica, sans-serif", fontSize: "16px"}} className="btn" onClick={async () => { 
        const res = await logout();
        if (res?.error) {
          alert(res.error);
        } else if (res?.success) {
          window.location.href = "/";
          alert("Successfully logged out!");
        }
     }}>Logout</button>
}
</file>

<file path="src/app/user/page.module.css">
.card {
    background: #0f1f1a;
    border-radius: 0.75rem;
    padding: 1.5rem;
    color: #e5f6ef;
  }
  
  .btn {
    background: #2ecc71;
    padding: 0.5rem 1rem;
    border-radius: 0.75rem;
    color: black;
    text-decoration: none;
    border: none;
    cursor: pointer;
  }
  
  .order {
    border-top: 1px solid #2ecc71;
    padding-top: 0.5rem;
    margin-top: 0.5rem;
  }
</file>

<file path="src/lib/db.ts">
import { DB } from "@/lib/db-types";
import SQLite from "better-sqlite3";
import { SqliteDialect, Kysely } from "kysely";

export default function getDB(){
  const dialect = new SqliteDialect({ database: new SQLite("db.sqlite") });
  const db = new Kysely<DB>({ dialect });
  return db
}
</file>

<file path="src/lib/hashing.ts">
import bcrypt from "bcrypt";

export function hashPassword(password: string) {
    return bcrypt.hash(password, 10).then(function(hash: string){
        return hash;
    });
}

export function checkPassword(password: string, hash: string) {
    return bcrypt.compare(password, hash).then(function(result: boolean){
        return result;
    });
}
</file>

<file path="src/lib/types.ts">
// lib/types.ts
import type { Selectable, Insertable } from "kysely";
import type { Users, Orders, Products, UserAddress } from "@/lib/db-types";

export type UserRow = Selectable<Users>;
export type OrderRow = Selectable<Orders>;
export type ProductRow = Selectable<Products>;
export type AddressRow = Selectable<UserAddress>;

export type AddressForm = Omit<
  Insertable<UserAddress>,
  "id" | "user_id"
>;
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts", "src/app/products"],
  "exclude": ["node_modules"]
}
</file>

<file path="migrations/1762706948911_create_ products_and_users.ts">
import { sql, type Kysely } from "kysely";

export async function up(db: Kysely<unknown>): Promise<void> {
	await sql`CREATE TABLE products (
		id integer primary key autoincrement not null,
		name text not null,
		description text not null,
		price integer not null,
		image text not null,
		category text not null
	) STRICT`.execute(db);

	await sql`CREATE TABLE users (
		id integer primary key autoincrement not null,
		username text not null unique,
		email text not null unique,
		password text not null
	) STRICT`.execute(db);

	await sql`CREATE TABLE favorite_products (
		id integer primary key autoincrement not null,
		user_id integer not null,
		product_id integer not null,
		foreign key(user_id) references users(id),
		foreign key(product_id) references products(id)
	) STRICT`.execute(db);
}
</file>

<file path="migrations/1763388106447_create_session.ts">
import { sql, type Kysely } from "kysely";

export async function up(db: Kysely<unknown>): Promise<void> {
	await sql`CREATE TABLE session (
		id integer primary key autoincrement not null,
		user_id integer not null,
		session_id text not null,
		expiration integer not null,
		foreign key(user_id) references users(id)
	) STRICT`.execute(db);
}
</file>

<file path="migrations/1764093601083_add_admin_col.ts">
import { sql, type Kysely } from "kysely";

export async function up(db: Kysely<unknown>): Promise<void> {
    await sql`ALTER TABLE users ADD COLUMN is_admin INTEGER DEFAULT 0`.execute(db);
}
</file>

<file path="migrations/1765999330032_create_order.ts">
import { sql, type Kysely } from "kysely";

export async function up(db: Kysely<unknown>): Promise<void> {
	await sql`CREATE TABLE orders (
		id integer primary key autoincrement not null,
		name text not null,
		surname text not null,
		street text not null,
		house_number text not null,
		city text not null,
		country text not null,
		zip_code text not null,
		phone_number text not null,
		email text not null,
		confirmed integer not null,
		user_id integer not null,
		foreign key(user_id) references users(id)
	) STRICT`.execute(db);
}
</file>

<file path="migrations/1766001491719_create_order_items.ts">
import { sql, type Kysely } from "kysely";

export async function up(db: Kysely<unknown>): Promise<void> {
	await sql`CREATE TABLE order_product (
		id integer primary key autoincrement not null,
		product_id integer not null,
		order_id integer not null,
		quantity integer not null default 1,
		foreign key(product_id) references products(id),
		foreign key(order_id) references orders(id)
	) STRICT`.execute(db);
}
</file>

<file path="migrations/1768321026001_add_user_address.ts">
import { sql, type Kysely } from "kysely";

export async function up(db: Kysely<unknown>): Promise<void> {
	await sql`CREATE TABLE user_address (
		id integer primary key autoincrement,
		user_id integer not null unique,
		street text not null,
		house_number text not null,
		city text not null,
		zip_code text not null,
		country text not null,
		foreign key (user_id) references users(id) on delete cascade
	) STRICT`.execute(db);
}
</file>

<file path="migrations/1768326003783_update_user_address.ts">
import { sql, type Kysely } from "kysely";

export async function up(db: Kysely<unknown>): Promise<void> {
    await sql`DROP TABLE IF EXISTS user_address`.execute(db);

    await sql`CREATE TABLE user_address (
        id integer primary key autoincrement,
        user_id integer not null unique,
        street text not null,
        house_number text not null,
        city text not null,
        zip_code text not null,
        country text not null,
        phone_number text,
        name text,
        surname text,
        foreign key (user_id) references users(id) on delete cascade
  ) STRICT`.execute(db);
}
</file>

<file path="seeds/1765049644188_new_seed.ts">
import { DB } from '@/lib/db-types';
import { hashPassword } from '../src/lib/hashing';
import { randomInt } from 'crypto';
import type { Kysely } from 'kysely'

export async function seed(db: Kysely<DB>): Promise<void> {
	await db.deleteFrom("favorite_products").execute();
	await db.deleteFrom("session").execute();
	await db.deleteFrom("orders").execute();
	await db.deleteFrom("order_product").execute();
	await db.deleteFrom("user_address").execute();
	await db.deleteFrom("users").execute();
	await db.deleteFrom("products").execute();

	const products = [
		// psy
		{ name: "Trixie Zubná Pasta pre Psy", description: "Pasta s mätovou príchuťou pre svieži dych.", category: "psy,starostlivosť" },
		{ name: "Royal Canin Medium Adult", description: "Kompletné krmivo pre dospelé psy stredných plemien.", category: "psy,krmivo" },
		{ name: "Pedigree Dentastix Medium", description: "Dentálna pochúťka pre ústnu hygienu psov.", category: "psy,maškrty" },
		{ name: "Brit Care Dog Show Champion", description: "Hypoalergénne granule pre psy vo výstavnej kondícii.", category: "psy,krmivo" },
		{ name: "Trixie Plyšový macko", description: "Mäkká hračka pre psov všetkých vekových kategórií.", category: "psy,hračky" },
		{ name: "Purina Pro Plan Small and Mini Adult", description: "Krmivo vyvinuté pre potreby malých a trpasličích plemien.", category: "psy,krmivo" },
		{ name: "Kong Classic Gumená Hračka", description: "Legendárna odolná hračka z prírodného kaučuku na plnenie maškrtami.", category: "psy,hračky" },
		{ name: "Julius-K9 IDC Postroj", description: "Profesionálny ergonomický postroj pre aktívne psy.", category: "psy,venčenie" },
		{ name: "Advantix Pipety pre psy", description: "Účinná ochrana proti kliešťom, blchám a komárom.", category: "psy,lekáreň" },
		{ name: "Brit Training Snacks L", description: "Chutné tréningové maškrty pre veľké plemená.", category: "psy,maškrty" },
		{ name: "Pedigree Junior Kapsička Hovädzia", description: "Šťavnaté mäsové kúsky v omáčke pre šteňatá.", category: "psy,krmivo" },
		{ name: "Trixie Chladniaca Podložka", description: "Podložka na reguláciu telesnej teploty počas horúcich dní.", category: "psy,doplnky" },
		{ name: "Chuckit Ultra Ball", description: "Vysoko odolná loptička s výborným odrazom pre aportovanie.", category: "psy,hračky" },
		{ name: "Hill's Science Plan Adult Light", description: "Krmivo s nízkym obsahom kalórií pre menej aktívne psy.", category: "psy,krmivo" },
		{ name: "Flexi New Classic Vodítko", description: "Samonavíjacie lanko s dĺžkou 5 metrov pre voľnosť pohybu.", category: "psy,venčenie" },

		// macky
		{ name: "Savic Záchod pre Mačky Nestor", description: "Krytá toaleta s filtrom proti zápachu.", category: "mačky" },
		{ name: "Royal Canin Kitten", description: "Špeciálne granule pre mačiatka v druhej fáze rastu.", category: "mačky,krmivo" },
		{ name: "Whiskas Kapsičky Klasický Výber", description: "Multipack kapsičiek s rôznymi druhmi mäsa v šťave.", category: "mačky,krmivo" },
		{ name: "Felix Fantastic s Kuracím", description: "Lahodné kúsky mäsa v želé, ktoré skvele voňajú.", category: "mačky,krmivo" },
		{ name: "Purina One Sterilcat", description: "Granule navrhnuté špeciálne pre kastrované mačky.", category: "mačky,krmivo" },
		{ name: "Brit Care Cat Grain-Free Sterilized", description: "Bezobilné krmivo pre citlivé kastrované mačky.", category: "mačky,krmivo" },
		{ name: "Trixie Škrabadlo Zaragoza", description: "Viacúrovňové škrabadlo s odpočívadlami a hračkou.", category: "mačky" },
		{ name: "Catit Fontána na Vodu Flower", description: "Automatická fontánka podporujúca pitný režim mačky.", category: "mačky,doplnky" },
		{ name: "GimCat Malt-Soft Pasta Professional", description: "Pasta na prirodzený odchod prehltnutých chlpov.", category: "mačky,lekáreň" },
		{ name: "Ever Clean Litter Podstielka", description: "Prémiová hrudkujúca podstielka s aktívnym uhlím.", category: "mačky" },
		{ name: "Animonda Carny Adult Hovädzie", description: "Kvalitná konzerva s vysokým obsahom čerstvého mäsa.", category: "mačky,krmivo" },
		{ name: "Royal Canin Urinary SO pre Mačky", description: "Veterinárna diéta pri ochoreniach močových ciest.", category: "mačky,krmivo" },
		{ name: "Sheba Selection v Omáčke", description: "Exkluzívne receptúry pre náročné mačacie jazýčky.", category: "mačky,krmivo" },
		{ name: "Cosma Nature Tuniak", description: "Doplnkové krmivo s 75% podielom čistého mäsa z tuniaka.", category: "mačky,krmivo" },

		// hlodavce
		{ name: "Versele-Laga Cuni Complete", description: "Extrudované krmivo pre králiky zabraňujúce selektívnemu kŕmeniu.", category: "hlodavce,krmivo" },
		{ name: "Brit Animals Rabbit Junior", description: "Superprémiové krmivo pre mladé králiky.", category: "hlodavce,krmivo" },
		{ name: "Trixie Drevený Domček", description: "Prírodný domček z borovicového dreva pre škrečky.", category: "hlodavce,doplnky" },
		{ name: "Vitakraft Kräcker Tyčinky", description: "Chrumkavé tyčinky s medom a špaldou pre králiky.", category: "hlodavce,maškrty" },
		{ name: "Savic Klietka pre Škrečka", description: "Priestranná klietka s tunelmi a kolotočom.", category: "hlodavce,klietky" },
		{ name: "Beaphar Care+ pre Potkany", description: "Veľmi chutné a vyvážené prémiové krmivo pre potkany.", category: "hlodavce,krmivo" },

		// akvaristika
		{ name: "TetraMin Vločkové Krmivo", description: "Zmes siedmich druhov vločiek pre zdravie a energiu rýb.", category: "akvaristika,krmivo" },
		{ name: "Eheim Vnútorný Filter Pickup", description: "Kompaktný a tichý filter pre akváriá do 60 litrov.", category: "akvaristika,technika" },
		{ name: "Sera Aquatan Kondicionér", description: "Prípravok na úpravu vodovodnej vody na bezpečnú vodu.", category: "akvaristika" },
		{ name: "Fluval LED Osvetlenie", description: "Moderné LED osvetlenie s nastaviteľným spektrom.", category: "akvaristika,technika,svetlá" },
		{ name: "Tetra Weekend Krmivo", description: "Kŕmne tablety, ktoré vydržia počas vašej neprítomnosti.", category: "akvaristika,krmivo" },
		{ name: "JBL ProNovo Bel Flakes", description: "Základné krmivo pre všetky spoločenské akváriové ryby.", category: "akvaristika,krmivo" },
		{ name: "Dennerle Nano DeponitMix", description: "Výživný substrát pre rastlinky v nano akváriách.", category: "akvaristika" },
		{ name: "Juvel Bioflow Filtračná vata", description: "Vata na zachytávanie hrubých nečistôt v akváriu.", category: "akvaristika,technika" },

		// vtaky
		{ name: "Versele-Laga Prestige Loro Parque", description: "Zmes semien obohatená o vitamíny pre papagáje.", category: "vtáky,krmivo" },
		{ name: "Vitakraft Menu pre Andulky", description: "Tradičné krmivo s jódom a minerálmi pre zdravie.", category: "vtáky,krmivo" },
		{ name: "Trixie Kúpeľnička pre Vtáky", description: "Plastová kúpeľnička s možnosťou zavesenia na klietku.", category: "vtáky,doplnky" },
		{ name: "Ferplast Klietka pre Kanáriky", description: "Elegantná klietka s výbavou (pítka, bidielka).", category: "vtáky,klietky" },
		{ name: "JR Farm Klasy Senegalského Prosa", description: "Obľúbená prírodná pochúťka pre všetky druhy vtákov.", category: "vtáky,maškrty" },
	];

	// zapis
	for (const product of products) {
		const imageName = product.name
			.toLowerCase()
			.replaceAll(' ', '_') + ".webp";

		await db.insertInto("products")
			.values({
				name: product.name,
				description: product.description,
				category: product.category,
				price: Math.round((randomInt(2, 80) + 0.99) * 100),
				image: imageName
			})
			.execute();
	}

	await db.insertInto("users")
		.values({
			username: "admin",
			email: "admin@petshop.sk",
			password: await hashPassword("admin123"),
			is_admin: 1
		})
		.execute();
}
</file>

<file path="src/actions/admin.ts">
"use server";

import getDB from "@/lib/db";
import { cookies } from "next/headers";
import { revalidatePath } from "next/cache";
import { redirect } from "next/navigation";
import { Resend } from "resend";
import { writeFile } from "fs/promises";

async function requireAdmin() {
    const cookieStore = await cookies();
    const sessionId = cookieStore.get("session")?.value;
    if (!sessionId) redirect("/");

    const db = getDB();

    const session = await db
        .selectFrom("session")
        .selectAll()
        .where("session_id", "=", sessionId)
        .executeTakeFirst();

    if (!session) redirect("/");

    const user = await db
        .selectFrom("users")
        .selectAll()
        .where("id", "=", session.user_id)
        .executeTakeFirst();

    if (!user || user.is_admin === 0) redirect("/");

    return { db, user };
}

export async function addProduct(name: string, description: string, category: string, price: number, image: File | null) {
    const { db } = await requireAdmin();

    if (!image) {
        return { error: "Image is required" };
    }

    const buffer = Buffer.from(await image.arrayBuffer());
    const fileName = `${Date.now()}_${image.name}`;

    await import("fs/promises").then(fs =>
        fs.writeFile(`public/uploads/${fileName}`, buffer)
    );

    await db
        .insertInto("products")
        .values({
            name,
            description,
            category,
            price,
            image: fileName,
        })
        .execute();

    revalidatePath("/admin");
    revalidatePath("/products");

    return { success: true };
}

export async function updateProduct(
    id: number,
    data: {
        name?: string;
        description?: string;
        category?: string;
        price?: number;
        image?: File;
    }
) {

    const { db } = await requireAdmin();

    let imageName: string | undefined;

    if (data.image) {
        const bytes = await data.image.arrayBuffer();
        const buffer = Buffer.from(bytes);
        imageName = Date.now() + "_" + data.image.name;

        await writeFile(
            `./public/uploads/${imageName}`,
            buffer
        );
    }

    await db
        .updateTable("products")
        .set({
            name: data.name,
            description: data.description,
            category: data.category,
            price: data.price,
            ...(imageName ? { image: imageName } : {}),
        })
        .where("id", "=", id)
        .execute();

    revalidatePath("/admin");
}

export async function deleteProduct(id: number) {
    const { db } = await requireAdmin();

    await db.deleteFrom("favorite_products").where("product_id", "=", id).execute();
    await db.deleteFrom("order_product").where("product_id", "=", id).execute();
    await db.deleteFrom("products").where("id", "=", id).execute();

    revalidatePath("/admin");
    revalidatePath("/products");
}

export async function deleteUserAndData(userId: number) {
    const { db, user } = await requireAdmin();

    if (user.id === userId) {
        return { error: "You cannot delete yourself." };
    }

    const target = await db
        .selectFrom("users")
        .selectAll()
        .where("id", "=", userId)
        .executeTakeFirst();

    if (!target || target.is_admin === 1) {
        return { error: "Cannot delete this user." };
    }

    await db.deleteFrom("favorite_products").where("user_id", "=", userId).execute();
    await db.deleteFrom("order_product")
        .where("order_id", "in",
            db.selectFrom("orders").select("id").where("user_id", "=", userId)
        )
        .execute();
    await db.deleteFrom("orders").where("user_id", "=", userId).execute();
    await db.deleteFrom("session").where("user_id", "=", userId).execute();
    await db.deleteFrom("users").where("id", "=", userId).execute();

    revalidatePath("/admin");
}

export async function markOrderAsSent(orderId: number, email: string) {
    const { db } = await requireAdmin();

    await db
        .updateTable("orders")
        .set({ confirmed: 1 })
        .where("id", "=", orderId)
        .execute();

    const resend = new Resend(process.env.RESEND_API_KEY!);

    await resend.emails.send({
        from: "Shop <onboarding@resend.dev>",
        to: email,
        subject: "Vaša objednávka je na ceste! 🚚",
        html: `
        <div style="font-family: Arial, sans-serif; line-height: 1.6; color: #0f1f1a; background-color: #f0f0f0; padding: 40px; margin: 0;">
          <div style="max-width: 600px; margin: 0 auto; background-color: #ffffff; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 10px rgba(0,0,0,0.08);">
            
            <div style="background-color: #22c55e; padding: 20px; text-align: center;">
              <h1 style="color: #ffffff; margin: 0; font-size: 24px;">Objednávka odoslaná</h1>
            </div>

            <div style="padding: 30px; text-align: center;">
              <div style="font-size: 48px; margin-bottom: 20px;">🚚</div>
              <p style="font-size: 18px; font-weight: bold; margin-top: 0;">
                Skvelé správy!
              </p>
              <p>Vaša objednávka č. <strong>${orderId}</strong> bola práve odoslaná a čoskoro bude u Vás.</p>
              
              <div style="margin: 30px 0;">
                <a href="#" style="background-color: #22c55e; color: white; padding: 12px 24px; text-decoration: none; border-radius: 12px; font-weight: bold;">
                  Sledovať balík
                </a>
              </div>

              <p style="font-size: 14px; color: #6b7280; margin-top: 20px;">
                Ak máte akékoľvek otázky, neváhajte nás kontaktovať odpoveďou na tento e-mail.
              </p>
            </div>

            <div style="background-color: #f9fafb; padding: 20px; text-align: center; font-size: 12px; color: #6b7280;">
              <p style="margin: 0;">&copy; 2026 Váš Obchod. Všetky práva vyhradené.</p>
            </div>
            
          </div>
        </div>
        `,
    });

    revalidatePath("/admin");
}

export async function deleteOrder(orderId: number) {
    const { db } = await requireAdmin();

    await db.deleteFrom("order_product").where("order_id", "=", orderId).execute();
    await db.deleteFrom("orders").where("id", "=", orderId).execute();

    revalidatePath("/admin");
}
</file>

<file path="src/actions/products.ts">
"use server"

import { cookies } from "next/headers"
import { getUser } from "./user"
import getDB from "@/lib/db"
import { revalidatePath } from "next/cache"
import { redirect } from "next/navigation"
import { Resend } from 'resend';
import { get } from "http"

export async function addToBasket(id: number) {
    const keksiky = await cookies()
    let kosik = keksiky.get("cart")?.value ?? "[]"

    if (kosik === "") {
        kosik = "[]"
    }

    const basket = JSON.parse(kosik)

    const item = basket.find((item: { id: number, quantity: number }) => item.id === id)

    if (item) {
        item.quantity += 1
        keksiky.set("cart", JSON.stringify(basket), { path: "/" })
        return
    }

    basket.push({
        id: id,
        quantity: 1
    })

    keksiky.set("cart", JSON.stringify(basket), { path: "/" })
}

export async function removeFromBasket(id: number) {
    const keksiky = await cookies()
    let kosik = keksiky.get("cart")?.value ?? "[]"

    if (kosik === "") {
        kosik = "[]"
    }

    const basket = JSON.parse(kosik)

    const item = basket.find((item: { id: number, quantity: number }) => item.id === id)

    if (item) {
        item.quantity -= 1
        keksiky.set("cart", JSON.stringify(basket), { path: "/" })
        if (item.quantity <= 0) {
            basket.splice(basket.indexOf(item), 1)
            keksiky.set("cart", JSON.stringify(basket), { path: "/" })
        }
    }
}

export async function isInFavourites(id: number): Promise<boolean> {
    const db = getDB();
    const user = await getUser();

    if (user == null) {
        return false;
    }

    const favourite = await db
        .selectFrom("favorite_products")
        .where("product_id", "=", id)
        .where("user_id", "=", user.id)
        .selectAll()
        .executeTakeFirst();

    if (favourite) return true

    return false
}

export async function addToFavourites(id: number) {
    const db = getDB();
    const user = await getUser();

    if (user == null) {
        return;
    }

    const favourite = await db
        .selectFrom("favorite_products")
        .where("product_id", "=", id)
        .where("user_id", "=", user.id)
        .selectAll()
        .executeTakeFirst();

    if (favourite) {
        return;
    }

    await db.insertInto("favorite_products").values({ user_id: user.id, product_id: id }).executeTakeFirst();
    revalidatePath("/products")
}

export async function removeFromFavourites(id: number) {
    const db = getDB();
    const user = await getUser();

    if (user == null) {
        return;
    }

    const favourite = await db
        .selectFrom("favorite_products")
        .where("product_id", "=", id)
        .where("user_id", "=", user.id)
        .selectAll()
        .executeTakeFirst();

    if (!favourite) {
        return;
    }

    await db
        .deleteFrom("favorite_products")
        .where("product_id", "=", id)
        .where("user_id", "=", user.id)
        .executeTakeFirst();

    revalidatePath("/products")
}

export async function makeTemporaryOrder(ulica: string, popisne_cislo_domu: string, mesto: string, zip_code: string, krajina: string, telefon: string, email: string, meno: string, priezvisko: string) {
    const keksiky = await cookies()
    let order = keksiky.get("order")?.value ?? ""
    const basket = keksiky.get("cart")?.value ?? "[]"

    if (order != "") {
        keksiky.set("order", "", { path: "/", maxAge: 0 })
    }

    order = JSON.stringify({
        "ulica": ulica,
        "popisne_cislo_domu": popisne_cislo_domu,
        "mesto": mesto,
        "zip_code": zip_code,
        "krajina": krajina,
        "telefon": telefon,
        "email": email,
        "meno": meno,
        "priezvisko": priezvisko,
        "cart": JSON.parse(basket)
    })

    keksiky.set("order", order, { path: "/" })
    redirect("/confirmation");
}

export async function makeOrder() {
    const cookieStore = await cookies();
    const orderCookie = cookieStore.get("order")?.value;
    const db = getDB();
    if (!orderCookie) return;
    const order = JSON.parse(orderCookie);

    const user = await getUser();
    if (user == null) return;

    const result = await db
        .insertInto("orders")
        .values({
            name: order.meno,
            surname: order.priezvisko,
            street: order.ulica,
            house_number: order.popisne_cislo_domu,
            city: order.mesto,
            country: order.krajina,
            zip_code: order.zip_code,
            phone_number: order.telefon,
            email: order.email,
            confirmed: 0,
            user_id: user.id,
        })
        .executeTakeFirst();

    const orderId = Number(result.insertId);

    if (!orderId) {
        return { error: "Failed to create order" };
    }

    let total_price = 0;

    for (const item of order.cart) {
        const product = await db
            .selectFrom("products")
            .where("id", "=", item.id)
            .selectAll()
            .executeTakeFirst();

        if (!product) continue;

        total_price += item.quantity * product.price;

        await db
            .insertInto("order_product")
            .values({
                order_id: orderId,
                product_id: item.id,
                quantity: item.quantity,
            })
            .execute();
    }

    const resend = new Resend(process.env.RESEND_API_KEY!);

    await resend.emails.send({
        from: "Resend <onboarding@resend.dev>",
        to: order.email,
        subject: "Potvrdenie objednávky",
        html: `
        <div style="font-family: Arial, sans-serif; line-height: 1.6; color: #0f1f1a; background-color: #f0f0f0; padding: 40px; margin: 0;">
          <div style="max-width: 600px; margin: 0 auto; background-color: #ffffff; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 10px rgba(0,0,0,0.08);">
            
            <div style="background-color: #22c55e; padding: 20px; text-align: center;">
              <h1 style="color: #ffffff; margin: 0; font-size: 24px;">Potvrdenie objednávky</h1>
            </div>
    
            <div style="padding: 30px;">
              <p style="font-size: 18px; font-weight: bold; margin-top: 0;">
                Ahoj ${order.meno} ${order.priezvisko},
              </p>
              <p>Ďakujeme za Vašu objednávku v našom obchode. Číslo vašej objednávky je <strong>${orderId}</strong>.</p>
              
              <div style="background-color: #f9fafb; border: 1px solid #e5e7eb; border-radius: 12px; padding: 20px; margin: 20px 0;">
                <p style="margin: 0; color: #4b5563;">Celková suma na zaplatenie:</p>
                <p style="font-size: 24px; font-weight: bold; color: #22c55e; margin: 5px 0;">${total_price / 100} €</p>
              </div>
    
              <h3 style="border-bottom: 2px solid #f0f0f0; padding-bottom: 10px; margin-top: 30px;">Doručovacia adresa</h3>
              <p style="margin-bottom: 0;">
                ${order.ulica}, ${order.popisne_cislo_domu}, ${order.mesto}
              </p>
            </div>
    
            <div style="background-color: #f9fafb; padding: 20px; text-align: center; font-size: 12px; color: #6b7280;">
              <p style="margin: 0;">&copy; 2026 Vaša Stránka. Všetky práva vyhradené.</p>
            </div>
            
          </div>
        </div>
        `,
    });

    cookieStore.delete("cart");
    cookieStore.delete("order");
    redirect("/confirmed");
}

export async function getFavoriteProducts() {
    const db = getDB();
    const user = await getUser();
    if (!user) return [];

    const favorites = await db
        .selectFrom("products")
        .innerJoin("favorite_products", "products.id", "favorite_products.product_id")
        .where("favorite_products.user_id", "=", user.id)
        .selectAll("products")
        .execute();

    return favorites.map(p => ({ ...p, is_in_fav: true }));
}
</file>

<file path="src/actions/user.ts">
"use server";

import getDB from "@/lib/db";
import { cookies } from "next/headers";
import { checkPassword, hashPassword } from "@/lib/hashing";
import validator from 'validator';
import { Resend } from "resend";
import { redirect } from "next/navigation";

export async function login(username: string, password: string) {
    const db = getDB();
    const keksiky = await cookies();

    async function generateSessionId() {
        while (true) {
            let result = '';
            const characters = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
            for (let i = 0; i < 10; i++) {
                result += characters.charAt(Math.floor(Math.random() * characters.length));
            }

            const check = await db
                .selectFrom("session")
                .where("session_id", "=", result)
                .selectAll()
                .executeTakeFirst();

            if (!check) return result;
        }
    }

    if (!username || !password) {
        return { error: "Missing fields" };
    }

    const user = await db
        .selectFrom("users")
        .where("username", "=", username)
        .selectAll()
        .executeTakeFirst();

    if (!user) {
        return { error: "Invalid credentials" };
    }

    if (!(await checkPassword(password, user.password))) {
        return { error: "Wrong password" };
    }

    let session = await db
        .selectFrom("session")
        .where("user_id", "=", user.id)
        .selectAll()
        .executeTakeFirst();

    if (!session) {
        await db
            .insertInto("session")
            .values({
                user_id: user.id,
                session_id: await generateSessionId(),
                expiration: Date.now() + 1000 * 60 * 60 * 24 * 7,
            })
            .execute();

        session = await db
            .selectFrom("session")
            .where("user_id", "=", user.id)
            .selectAll()
            .executeTakeFirst();
    } else if (session.expiration < Date.now()) {
        await db
            .updateTable("session")
            .set({ expiration: Date.now() + 1000 * 60 * 60 * 24 * 7 })
            .where("user_id", "=", user.id)
            .execute();
    }

    if (session) {
        keksiky.set("session", session.session_id, {
            httpOnly: true,
            secure: process.env.NODE_ENV === "production",
            sameSite: "lax",
            path: "/",
            maxAge: 60 * 60 * 24 * 7
        });
    }

    return { success: true };
}

export async function logout() {
    const keksiky = await cookies()
    const sessionId = keksiky.get("session")?.value

    if (!sessionId) {
        return { error: "Not logged in, did not find session id" }
    }

    const db = getDB()

    const session = await db
        .selectFrom("session")
        .selectAll()
        .where("session_id", "=", sessionId)
        .executeTakeFirst()

    if (!session) {
        return { error: "session not found" }
    }

    await db
        .deleteFrom("session")
        .where("session_id", "=", sessionId)
        .execute()

    keksiky.delete("session");

    return { success: true }
}

export async function register(email: string, username: string, password: string) {
    const db = getDB();
    if (!email || !username || !password) {
        return { error: "Missing fields" };
    }

    const name_check = await db
        .selectFrom("users")
        .where("username", "=", username)
        .selectAll()
        .executeTakeFirst();

    if (name_check) {
        return { error: "Username already exists" };
    }

    const email_check = await db
        .selectFrom("users")
        .where("email", "=", email)
        .selectAll()
        .executeTakeFirst();

    if (email_check) {
        return { error: "An account with this email already exists" };
    }

    if (password.length < 8) {
        return { error: "Password must be at least 8 characters long" };
    }

    if (!validator.isEmail(email)) {
        return { error: "Invalid email" };
    }

    await db
        .insertInto("users")
        .values({
            email: email,
            username: username,
            password: await hashPassword(password),
        })
        .execute();

    return { success: true };
}

export async function getUser() {
    const db = getDB();
    const keksiky = await cookies()
    const sessionId = keksiky.get("session")?.value

    if (!sessionId) {
        return null;
    }

    const session = await db
        .selectFrom("session")
        .where("session_id", "=", sessionId)
        .selectAll()
        .executeTakeFirst();

    if (!session) {
        return null;
    }

    if (session.expiration < Date.now()) {
        logout();
        return null;
    }

    const user = await db
        .selectFrom("users")
        .where("id", "=", session.user_id)
        .selectAll()
        .executeTakeFirst();

    if (!user) {
        return null;
    }

    return user;
}

export async function passwordChange() {
    const resend = new Resend(process.env.RESEND_API_KEY!);
    const keksiky = await cookies();
    const user = await getUser();

    if (!user) {
        return;
    }

    const randomNumber = Math.floor(1000 + Math.random() * 9000);
    const stringNumber = randomNumber.toString();
    const hashedNumber = await hashPassword(stringNumber)

    keksiky.set("number", hashedNumber, {
        httpOnly: true,
        secure: process.env.NODE_ENV === "production",
        sameSite: "lax",
        path: "/",
        maxAge: 300
    });

    await resend.emails.send({
        from: "Resend <onboarding@resend.dev>",
        to: user.email,
        subject: "Password change",
        html: "<p>Your code is: " + stringNumber + "</p>",
    });

    redirect("/password_change2");
}

export async function passwordChange3(formData: FormData) {
    const db = getDB();
    const user = await getUser();
    const password1 = formData.get("password1") as string | null;
    const password2 = formData.get("password2") as string | null;

    if (!user || !password1 || !password2) {
        return;
    }

    if (password1 !== password2) {
        return;
    }

    if (password1.length < 8) {
        return;
    }

    const hashedPassword = await hashPassword(password1 as string);
    await db
        .updateTable("users")
        .set({ password: hashedPassword })
        .where("id", "=", user.id)
        .execute();

    redirect("/password_change4");
}

export async function updateAddress(formData: FormData) {
    const user = await getUser();
    if (!user) return;

    const db = getDB();

    const data = {
        street: formData.get("street") as string,
        house_number: formData.get("house_number") as string,
        city: formData.get("city") as string,
        zip_code: formData.get("zip_code") as string,
        country: formData.get("country") as string,
        phone_number: formData.get("phone_number") as string,
        name: formData.get("name") as string,
        surname: formData.get("surname") as string,
    };

    const existing = await db
        .selectFrom("user_address")
        .select("id")
        .where("user_id", "=", user.id)
        .executeTakeFirst();

    if (existing) {
        await db
            .updateTable("user_address")
            .set(data)
            .where("user_id", "=", user.id)
            .execute();
    } else {
        await db
            .insertInto("user_address")
            .values({ ...data, user_id: user.id })
            .execute();
    }

    redirect("/user");
}

export async function updateEmail(newEmail: string) {
    const db = getDB();
    const user = await getUser();
    if (!user) return { error: "Not logged in" };

    if (!validator.isEmail(newEmail)) return { error: "Invalid email" };

    const exists = await db
        .selectFrom("users")
        .where("email", "=", newEmail)
        .selectAll()
        .executeTakeFirst();

    if (exists) return { error: "Email already in use" };

    await db
        .updateTable("users")
        .set({ email: newEmail })
        .where("id", "=", user.id)
        .execute();

    return { success: true };
}


export async function verifyCode(input: string) {
    const keksiky = await cookies();
    const code = keksiky.get("number")?.value ?? "";
    const isCodeValid = await checkPassword(input, code);

    if (isCodeValid) {
        return { success: true };
    } else {
        return { error: "Invalid code" };
    }
}
</file>

<file path="src/app/admin/AddProductForm.tsx">
"use client";

import { useState } from "react";
import { addProduct } from "@/actions/admin";

export default function AddProductForm() {
  const [name, setName] = useState("");
  const [description, setDescription] = useState("");
  const [category, setCategory] = useState("");
  const [price, setPrice] = useState<number>(0);
  const [image, setImage] = useState<File | null>(null);

  return (
    <section className="admin-box">
      <h2>Add product</h2>

      <input placeholder="Name" onChange={e => setName(e.target.value)} />
      <input placeholder="Description" onChange={e => setDescription(e.target.value)} />
      <input placeholder="Category (comma separated)" onChange={e => setCategory(e.target.value)} />
      <input type="number" placeholder="Price" onChange={e => setPrice(Number(e.target.value))} />
      <input type="file" onChange={e => setImage(e.target.files?.[0] ?? null)} />

      <button
        onClick={async () => {
          const res = await addProduct(name, description, category, price, image);
          if (res?.error) alert(res.error);
          else alert("Produkt pridaný");
        }}
      >
        Add product
      </button>
    </section>
  );
}
</file>

<file path="src/app/admin/ProductEditor.tsx">
"use client";

import { useState } from "react";
import type { Products } from "@/lib/db-types";
import { deleteProduct, updateProduct } from "@/actions/admin";
import type { Selectable } from "kysely";

export default function ProductEditor({
  products,
}: {
  products: Selectable<Products>[];
}) {
  const [editing, setEditing] = useState<Selectable<Products> | null>(null);
  const [newImage, setNewImage] = useState<File | null>(null);

  // Helper to handle field updates immutably
  const handleFieldChange = (field: keyof Selectable<Products>, value: string | number) => {
    if (!editing) return;
    setEditing({
      ...editing,
      [field]: value,
    });
  };

  return (
    <section className="admin-box">
      <h2>Products</h2>

      {products.map((p) => (
        <div key={p.id} className="admin-row">
          <span>{p.name}</span>
          <button onClick={() => setEditing(p)}>Edit</button>
          <button onClick={() => deleteProduct(p.id)}>Delete</button>
        </div>
      ))}

      {editing && (
        <dialog open>
          <h3>Edit product</h3>

          <input
            placeholder="Name"
            value={editing.name}
            onChange={(e) => handleFieldChange("name", e.target.value)}
          />
          <input
            placeholder="Description"
            value={editing.description ?? ""}
            onChange={(e) => handleFieldChange("description", e.target.value)}
          />
          <input
            placeholder="Category"
            value={editing.category ?? ""}
            onChange={(e) => handleFieldChange("category", e.target.value)}
          />
          <input
            type="number"
            placeholder="Price"
            value={editing.price}
            onChange={(e) => handleFieldChange("price", Number(e.target.value))}
          />
          <input
            type="file"
            onChange={(e) => {
              const file = e.target.files?.[0] ?? null;
              setNewImage(file);
            }}
          />

          <div className="admin-actions">
            <button
              onClick={async () => {
                if (!editing) return;

                await updateProduct(editing.id, {
                  name: editing.name,
                  description: editing.description,
                  category: editing.category,
                  price: editing.price,
                  image: newImage ? newImage : undefined,
                });

                setEditing(null);
                setNewImage(null);
              }}
            >
              Save
            </button>
            <button onClick={() => setEditing(null)}>Cancel</button>
          </div>
        </dialog>
      )}
    </section>
  );
}
</file>

<file path="src/app/basket/page.tsx">
import getDB from "@/lib/db";
import { cookies } from "next/headers";
import RemoveFromCartButton from "./removeFromCartButton";
import Link from "next/link";

export default async function BasketPage() {
  const keksiky = await cookies();
  const kosik = keksiky.get("cart")?.value ?? "[]";
  
  if (kosik === "" || kosik === "[]") {
    return (
      <main style={{ padding: 32, display: "flex", flexDirection: "column", alignItems: "center" }}>
        <h1 style={{ fontSize: 32, fontWeight: 700, marginBottom: 24 }}>Basket</h1>
        <p style={{ color: "#555" }}>Your basket is empty.</p>
      </main>
    );
  }

  const kosik2 = JSON.parse(kosik);
  const products = await getDB()
    .selectFrom("products")
    .where("id", "in", kosik2.map((item: { id: number }) => item.id))
    .selectAll()
    .execute();

  const products_with_quantity = products.map(product => ({
    ...product,
    price: product.price / 100,
    quantity: kosik2.find((item: { id: number }) => item.id === product.id)?.quantity || 0,
  }));

  const total_price = products_with_quantity.reduce((acc, p) => acc + p.price * p.quantity, 0);

  return (
    <main style={{ padding: 32, display: "flex", flexDirection: "column", alignItems: "center" }}>
      <h1 style={{ fontSize: 32, fontWeight: 700, marginBottom: 24 }}>Basket</h1>

      <div
        style={{
          borderRadius: 12,
          overflow: "hidden",
          width: "100%",
          maxWidth: 800,
          boxShadow: "0 2px 8px rgba(0,0,0,0.1)",
        }}
      >
        {/* Table Header */}
        <div
          style={{
            display: "flex",
            backgroundColor: "#4CAF50",
            color: "white",
            padding: "12px 16px",
            fontWeight: 600,
          }}
        >
          <div style={{ flex: 3 }}>Product</div>
          <div style={{ flex: 1, textAlign: "center" }}>Quantity</div>
          <div style={{ flex: 1, textAlign: "center" }}>Price (€)</div>
          <div style={{ flex: 1, textAlign: "center" }}>Remove</div>
        </div>

        {/* Table Rows */}
        {products_with_quantity.map((product, i) => (
          <div
            key={product.id}
            style={{
              display: "flex",
              backgroundColor: i % 2 === 0 ? "#f5f5f5" : "#e0e0e0",
              padding: "12px 16px",
              alignItems: "center",
            }}
          >
            <div style={{ flex: 3, fontWeight: 500 }}>{product.name}</div>
            <div style={{ flex: 1, textAlign: "center" }}>{product.quantity}</div>
            <div style={{ flex: 1, textAlign: "center" }}>{(product.price * product.quantity).toFixed(2)}</div>
            <div style={{ flex: 1, textAlign: "center" }}>
              <RemoveFromCartButton
                id={product.id}
              />
            </div>
          </div>
        ))}

        {/* Table Footer / Total */}
        <div
          style={{
            display: "flex",
            justifyContent: "flex-end",
            padding: "12px 16px",
            fontWeight: 600,
            fontSize: 18,
            backgroundColor: "#f0f0f0",
          }}
        >
          Total: {total_price.toFixed(2)} €
        </div>
      </div>

      <Link
        href="/shipping_info"
        style={{
          marginTop: 24,
          backgroundColor: "#4CAF50",
          color: "white",
          padding: "12px 24px",
          borderRadius: 12,
          textDecoration: "none",
          fontWeight: 600,
        }}
      >
        Checkout
      </Link>
    </main>
  );
}
</file>

<file path="src/app/confirmation/orderButton.tsx">
"use client"
import { makeOrder } from "@/actions/products";

export default function OrderButton() {
    return (
        <button
            onClick={makeOrder}
            style={{
                margin: "1rem",
            }}
            className="btn"
        >
            Objednať
        </button>
    )
}
</file>

<file path="src/app/confirmation/page.tsx">
// confirmation/page.tsx
import getDB from "@/lib/db";
import { cookies } from "next/headers";
import OrderButton from "./orderButton";

interface CartItem {
  id: number;
  quantity: number;
}

interface OrderCookie {
  ulica: string;
  popisne_cislo_domu: string;
  mesto: string;
  zip_code: string;
  krajina: string;
  telefon: string;
  email: string;
  meno: string;
  priezvisko: string;
}

export default async function ConfirmationPage() {
  const cookieStore = await cookies();
  const orderRaw = cookieStore.get("order")?.value ?? "";
  const cartRaw = cookieStore.get("cart")?.value ?? "[]";

  if (!orderRaw || cartRaw === "[]") {
    return (
      <main
        style={{
          padding: "2rem",
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
        }}
      >
        <h1 style={{ fontSize: "2rem", fontWeight: 700 }}>Confirmation</h1>
        <p style={{ color: "#666", marginTop: "1rem" }}>
          Nothing to confirm.
        </p>
      </main>
    );
  }

  const order: OrderCookie = JSON.parse(orderRaw) as OrderCookie;
  const cart: CartItem[] = JSON.parse(cartRaw) as CartItem[];

  const db = getDB();

  const products = await db
    .selectFrom("products")
    .selectAll()
    .where(
      "id",
      "in",
      cart.map((item) => item.id)
    )
    .execute();

    const productsFixPrice = products.map((product) => ({
      ...product,
      price: product.price / 100,
    }));

  const rows = productsFixPrice.map((product) => {
    const cartItem = cart.find((c) => c.id === product.id);
    const quantity = cartItem ? cartItem.quantity : 0;
    const total = product.price * quantity;

    return {
      id: product.id,
      name: product.name,
      price: product.price,
      quantity,
      total,
    };
  });

  const grandTotal = rows.reduce((sum, row) => sum + row.total, 0);

  return (
    <main
      style={{
        padding: "2rem",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        gap: "2rem",
      }}
    >
      <h1 style={{ fontSize: "2rem", fontWeight: 700 }}>Confirmation</h1>

      {/* Shipping info */}
      <div
        style={{
          width: "100%",
          maxWidth: 700,
          background: "#f3f3f3",
          borderRadius: "0.75rem",
          padding: "1.5rem",
        }}
      >
        <h2 style={{ fontWeight: 600, marginBottom: "0.75rem" }}>
          Shipping information
        </h2>
        <p>{order.ulica} {order.popisne_cislo_domu}, {order.zip_code} {order.mesto}, {order.krajina}</p>
        <h2 style={{ fontWeight: 600, marginBottom: "0.75rem" }}>
          Contact information
        </h2>
        <p>Email: {order.email}</p>
        <p>Phone: {order.telefon}</p>
        <p>Name: {order.meno} {order.priezvisko}</p>
      </div>

      {/* Products table */}
      <div
        style={{
          width: "100%",
          maxWidth: 700,
          borderRadius: "0.75rem",
          overflow: "hidden",
        }}
      >
        {/* Header */}
        <div
          style={{
            display: "grid",
            gridTemplateColumns: "2fr 1fr 1fr 1fr",
            background: "#e0e0e0",
            padding: "0.75rem",
            fontWeight: 600,
          }}
        >
          <div>Product</div>
          <div>Price</div>
          <div>Quantity</div>
          <div>Total</div>
        </div>

        {/* Rows */}
        {rows.map((row, index) => (
          <div
            key={row.id}
            style={{
              display: "grid",
              gridTemplateColumns: "2fr 1fr 1fr 1fr",
              padding: "0.75rem",
              background: index % 2 === 0 ? "#f7f7f7" : "#ebebeb",
            }}
          >
            <div>{row.name}</div>
            <div>{row.price} €</div>
            <div>{row.quantity}</div>
            <div>{row.total} €</div>
          </div>
        ))}

        {/* Grand total */}
        <div
          style={{
            display: "grid",
            gridTemplateColumns: "2fr 1fr 1fr 1fr",
            padding: "0.75rem",
            background: "#dcdcdc",
            fontWeight: 700,
          }}
        >
          <div>Total</div>
          <div />
          <div />
          <div>{grandTotal} €</div>
        </div>
      </div>

      <OrderButton />
    </main>
  );
}
</file>

<file path="src/app/confirmed/page.tsx">
export default function ConfirmationPage() {
    return (
        <h1 style={{ textAlign: "center", marginTop: "5rem" }}>Potvrdenie bolo poslané na váš mail</h1>
    );
}
</file>

<file path="src/app/footer.module.css">
.footer {
    margin-top: 4rem;
    padding: 2.5rem 3rem 1.5rem;
    background-color: #f9fafb;
    border-top-left-radius: 0.75rem;
    border-top-right-radius: 0.75rem;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.548);
    margin-top: auto;
  }
  
  .content {
    display: flex;
    justify-content: space-evenly;
    gap: 3rem;
  }
  
  .section {
    max-width: 300px;
  }
  
  .section h3 {
    font-size: 1.4rem;
    margin-bottom: 0.6rem;
  }
  
  .section h4 {
    font-size: 1.1rem;
    margin-bottom: 0.6rem;
  }
  
  .section p {
    font-size: 0.95rem;
    color: #374151;
  }
  
  .section ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .section li {
    margin-bottom: 0.4rem;
  }
  
  .section a {
    text-decoration: none;
    color: #1f2937;
    padding: 0.3rem 0.5rem;
    border-radius: 0.75rem;
    transition: background-color 0.2s ease, color 0.2s ease;
  }
  
  .section a:hover {
    background-color: #22c55e;
    color: white;
  }
  
  .bottom {
    margin-top: 2rem;
    padding-top: 1rem;
    border-top: 1px solid #e5e7eb;
    text-align: center;
    font-size: 0.85rem;
    color: #6b7280;
  }
</file>

<file path="src/app/footer.tsx">
import styles from "./footer.module.css";

export default function Footer() {
    return (
        <footer className={styles.footer}>
            <div className={styles.content}>
                <div className={styles.section}>
                    <h3>Pet Shop</h3>
                    <p>
                        Všetko, čo váš maznáčik potrebuje, od krmiva až po hračky. Starostlivo vybrané produkty pre šťastné a zdravé zvieratá.
                    </p>
                </div>

                <div className={styles.section}>
                    <h4>Contact</h4>
                    <p>Email: support@petshop.com</p>
                    <p>Phone: +421 123 456 789</p>
                </div>
            </div>

            <div className={styles.bottom}>
                <p>© {new Date().getFullYear()} Pet Shop. All rights reserved.</p>
            </div>
        </footer>
    );
}
</file>

<file path="src/app/login/loginButton.tsx">
"use client"

import { login } from "@/actions/user";
import styles from "../form.module.css";

export default function LoginButton(props: { username: string; password: string }) {
    async function handleLogin() {
        const res = await login(props.username, props.password);
        if (res?.error) {
            alert(res.error);
        } else if (res?.success) {
            window.location.href = "/user";
            alert("Successfully logged in!");
        }
    }
    return (
        <button onClick={handleLogin} className={styles.btn}>Sign in</button>
    )
}
</file>

<file path="src/app/login/page.tsx">
"use client"

import { useState } from "react"
import LoginButton from "./loginButton";
import styles from "../form.module.css"; // Importing the new module

export default function LoginPage() {
  const [username, setUsername] = useState("")
  const [password, setPassword] = useState("")

  return (
    <div className={styles.container}>
      <h1 className={styles.title}>Login</h1>

      <form
        className={styles.form}
        onSubmit={(e) => e.preventDefault()}
      >
        <label className={styles.field}>
          Username
          <input
            type="text"
            className={styles.input}
            placeholder="Enter your username"
            value={username}
            onChange={(e) => setUsername(e.target.value)}
          />
        </label>

        <label className={styles.field}>
          Password
          <input
            type="password"
            className={styles.input}
            placeholder="Enter your password"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
          />
        </label>

        <LoginButton username={username} password={password} />
      </form>
    </div>
  );
}
</file>

<file path="src/app/navbar.tsx">
import getDB from "@/lib/db";
import { cookies } from "next/headers";
import Link from "next/link";
import styles from "./navbar.module.css";

export default async function NavBar() {
  const keksiky = await cookies();
  const sessionId = keksiky.get("session")?.value;
  const db = getDB();

  const defaultReturn = (
    <nav className={styles.navbar}>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/">Domov</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/products">Produkty</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/login">Prihlásenie</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/register">Registrácia</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/basket">Košík</Link>
      </div>
    </nav>
  );

  const adminReturn = (
    <nav className={styles.navbar}>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/">Domov</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/products">Produkty</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/admin">Admin</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/user">Vaše konto</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/basket">Košík</Link>
      </div>
    </nav>
  );

  const userReturn = (
    <nav className={styles.navbar}>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/">Domov</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/products">Produkty</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/user">Vaše konto</Link>
      </div>
      <div className={styles.navItem}>
        <Link className={styles.navLink} href="/basket">Košík</Link>
      </div>
    </nav>
  );

  if (!sessionId) {
    return defaultReturn;
  }

  const session = await db
    .selectFrom("session")
    .selectAll()
    .where("session_id", "=", sessionId)
    .executeTakeFirst();

  if (!session) {
    return defaultReturn;
  }

  const user = await db
    .selectFrom("users")
    .selectAll()
    .where("id", "=", session.user_id)
    .executeTakeFirst();

  if (!user) {
    return defaultReturn;
  }

  if (user.is_admin) {
    return adminReturn;
  } else {
    return userReturn;
  }
}
</file>

<file path="src/app/page.tsx">
import getDB from "@/lib/db";
import HeroCarousel from "./carousel/HeroCarousel";
import styles from "./page.module.css";
import { getUser } from "@/actions/user";
import SingleProduct from "./products/singleProduct";
import { isInFavourites } from "@/actions/products";

export default async function Home() {
    const latestProducts = await getDB()
        .selectFrom("products")
        .selectAll()
        .orderBy("id", "desc")
        .limit(4)
        .execute();

    const productsWithFav = [];

    for (const product of latestProducts) {
        const is_in_fav = await isInFavourites(product.id);
        productsWithFav.push({ ...product, is_in_fav });
    }

    const user = await getUser();


    return (
        <main className={styles.home}>
            <HeroCarousel />

            <section className={styles.latest}>
                <h2>Our latest additions</h2>

                <div className={styles.productRow}>
                    {productsWithFav.map((product) => (
                        <SingleProduct key={product.id} product={product} userLoggedIn={!!user} />
                    ))}
                </div>
            </section>
        </main>
    );
}
</file>

<file path="src/app/password_change2/page.tsx">
"use client"

import { useState } from "react"
import VerifyButton from "./verifyButton";
import styles from "../form.module.css";

export default function LoginPage() {
  const [code, setCode] = useState("")

  return (
    <div className={styles.container}>
      <h1 className={styles.title}>Verify code</h1>

      <form
        className={styles.form}
        onSubmit={(e) => e.preventDefault()}
      >
        <label className={styles.field}>
          Code
          <input
            type="text"
            className={styles.input}
            placeholder="Enter the code"
            value={code}
            onChange={(e) => setCode(e.target.value)}
          />
        </label>

        <VerifyButton code={code} />
      </form>
    </div>
  );
}
</file>

<file path="src/app/password_change3/page.tsx">
"use client"
import { passwordChange3 } from "@/actions/user";

export default function PasswordChange3Page() {
  return (
    <form
      action={passwordChange3}
      className="card max-w-md mx-auto flex flex-col gap-4"
    >
      <h1>Set New Password</h1>
      <input
        name="password1"
        type="password"
        placeholder="New password"
        required
        className="border p-2 rounded"
      />
      <input
        name="password2"
        type="password"
        placeholder="Repeat password"
        required
        className="border p-2 rounded"
      />
      <button type="submit" className="btn">Save</button>
    </form>
  );
}
</file>

<file path="src/app/products/products.module.css">
.page {
    padding: 2rem 3rem;
  }
  
  .filters {
    display: flex;
    gap: 0.8rem;
    margin-bottom: 2rem;
    flex-wrap: wrap;
  }
  
  .filterButton {
    padding: 0.4rem 0.9rem;
    border-radius: 0.75rem;
    border: 1px solid #e5e7eb;
    background: white;
    cursor: pointer;
    transition: background-color 0.2s ease, color 0.2s ease;
    text-transform: capitalize;
  }
  
  .filterButton:hover {
    background-color: #22c55e;
    color: white;
  }
  
  .active {
    background-color: #22c55e;
    color: white;
    border-color: #22c55e;
  }
  
  .grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
    gap: 2rem;
  }
  
  .card {
    width: 260px;
    background: white;
    border-radius: 0.75rem;
    padding: 1.5rem;
    text-align: center;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
  }
  
  .card h3 {
    margin: 0.8rem 0 0.4rem;
  }
  
  .description {
    font-size: 0.9rem;
    color: #4b5563;
    margin-bottom: 0.6rem;
  }
  
  .price {
    font-weight: 600;
    margin-bottom: 1rem;
  }
  
  .actions {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  
  .actions button {
    padding: 0.4rem;
    border-radius: 0.75rem;
    border: none;
    cursor: pointer;
    background-color: #e5e7eb;
    transition: background-color 0.2s ease, color 0.2s ease;
  }
  
  .actions button:hover {
    background-color: #22c55e;
    color: white;
  }
</file>

<file path="src/app/products/ProductsClient.tsx">
"use client";

import { useState } from "react";
import styles from "./products.module.css";
import SingleProduct from "./singleProduct";

interface ProductWithFav {
    id: number;
    name: string;
    description: string;
    image: string;
    price: number;
    category: string;
    is_in_fav: boolean;
}

interface ProductsClientProps {
    products: ProductWithFav[];
    categories: string[];
    userLoggedIn: boolean;
}

export default function ProductsClient({
    products,
    categories,
    userLoggedIn,
}: ProductsClientProps) {
    const [selectedCategories, setSelectedCategories] = useState<string[]>([]);

    const toggleCategory = (cat: string) => {
        setSelectedCategories((prev) =>
            prev.includes(cat)
                ? prev.filter((c) => c !== cat) // Remove if already selected
                : [...prev, cat]                // Add if not selected
        );
    };

    const filteredProducts = selectedCategories.length > 0
        ? products.filter((p: ProductWithFav) => {
            const productCats = p.category.split(",").map((c: string) => c.trim());
            return selectedCategories.every((selected) => productCats.includes(selected));
        })
        : products;

    return (
        <main className={styles.page}>
            <h1>Products</h1>

            <div className={styles.filters}>
                <button
                    className={`${styles.filterButton} ${selectedCategories.length === 0 ? styles.active : ""}`}
                    onClick={() => setSelectedCategories([])}
                >
                    All
                </button>

                {categories.map((cat: string) => (
                    <button
                        key={cat}
                        className={`${styles.filterButton} ${selectedCategories.includes(cat) ? styles.active : ""
                            }`}
                        onClick={() => toggleCategory(cat)}
                    >
                        {cat}
                    </button>
                ))}
            </div>

            {filteredProducts.length === 0 ? (
                <div className={styles.noProducts}>
                    <p>No products match the selected filters.</p>
                </div>
            ) : (
                <div className={styles.grid}>
                    {filteredProducts.map((product: ProductWithFav) => (
                        <SingleProduct key={product.id} product={product} userLoggedIn={userLoggedIn} />
                    ))}
                </div>
            )}
        </main>
    );
}
</file>

<file path="src/app/register/page.tsx">
"use client"

import { useState } from "react"
import RegisterButton from "./registerButton";
import styles from "../form.module.css"; // Adjust path based on your file location

export default function RegisterPage() {
  const [mail, setMail] = useState("")
  const [username, setUsername] = useState("")
  const [password, setPassword] = useState("")

  return (
    <div className={styles.container}>
      <h1 className={styles.title}>Register</h1>

      <form
        className={styles.form}
        onSubmit={(e) => e.preventDefault()}
      >
        <label className={styles.field}>
          Email
          <input
            type="email"
            className={styles.input}
            placeholder="example@mail.com"
            value={mail}
            onChange={(e) => setMail(e.target.value)}
          />
        </label>

        <label className={styles.field}>
          Username
          <input
            type="text"
            className={styles.input}
            placeholder="Choose a username"
            value={username}
            onChange={(e) => setUsername(e.target.value)}
          />
        </label>

        <label className={styles.field}>
          Password
          <input
            type="password"
            className={styles.input}
            placeholder="Create a password"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
          />
        </label>

        {/* The RegisterButton can now be styled to match the green .btn in your globals */}
        <RegisterButton username={username} password={password} email={mail} />
      </form>
    </div>
  );
}
</file>

<file path="src/app/register/registerButton.tsx">
"use client"

import { login, register } from "@/actions/user";
import styles from "../form.module.css";

export default function RegisterButton(props: { email: string; username: string; password: string; }) {

    async function handleRegister() {
        const res = await register(props.email, props.username, props.password);
        if (res?.error) {
            alert(res.error);
        } else if (res?.success) {
            const res2 = await login(props.username, props.password);
            if (res2?.error) {
                alert(res2.error);
            } else if (res2?.success) {
                window.location.href = "/user";
                alert("Successfully registered and automatically logged in!");
            }
        }
    }

    return (
        <button className={styles.btn} onClick={handleRegister}>
            Register
        </button>
    )
}
</file>

<file path="src/app/shipping_info/LinkToConfirmation.tsx">
"use client"
import { makeTemporaryOrder } from "@/actions/products";

export default function LinkToConfirmation() {
    async function go() {
        const ulica = (document.getElementById("ulica") as HTMLInputElement).value;
        const popisne_cislo_domu = (document.getElementById("popisne_cislo_domu") as HTMLInputElement).value;
        const mesto = (document.getElementById("mesto") as HTMLInputElement).value;
        const zip_code = (document.getElementById("zip_code") as HTMLInputElement).value;
        const krajina = (document.getElementById("krajina") as HTMLInputElement).value;
        const telefon = (document.getElementById("telefon") as HTMLInputElement).value;
        const email = (document.getElementById("email") as HTMLInputElement).value;
        const meno = (document.getElementById("meno") as HTMLInputElement).value;
        const priezvisko = (document.getElementById("priezvisko") as HTMLInputElement).value;

        if (!ulica || !popisne_cislo_domu || !mesto || !zip_code || !krajina || !telefon || !email || !meno || !priezvisko) {
            alert("Musia byť vyplnené všetky polia");
            return;
        };

        await makeTemporaryOrder(
            ulica,
            popisne_cislo_domu,
            mesto,
            zip_code,
            krajina,
            telefon,
            email,
            meno,
            priezvisko
        );
    }

    return (
        <button
            onClick={go}
            className="btn"
        >
            Continue to Confirmation
        </button>
    )
}
</file>

<file path="src/app/shipping_info/page.tsx">
import getDB from "@/lib/db";
import { cookies } from "next/headers";
import LinkToConfirmationButton from "./LinkToConfirmation";
import { updateAddress } from "@/actions/user";
import styles from "../form.module.css"; // Adjust path as necessary

export default async function ShippingInfoPage() {
  const db = getDB();
  const keksiky = await cookies();
  const sessionId = keksiky.get("session")?.value;

  let userData: {
    email?: string;
    street?: string;
    house_number?: string;
    city?: string;
    zip_code?: string;
    country?: string;
    phone_number?: string;
    name?: string;
    surname?: string;
  } = {};

  if (sessionId) {
    const session = await db
      .selectFrom("session")
      .selectAll()
      .where("session_id", "=", sessionId)
      .executeTakeFirst();

    if (session) {
      const user = await db
        .selectFrom("users")
        .selectAll()
        .where("id", "=", session.user_id)
        .executeTakeFirst();

      const address = await db
        .selectFrom("user_address")
        .selectAll()
        .where("user_id", "=", session.user_id)
        .executeTakeFirst();

      if (user || address) {
        userData = {
          email: user?.email ?? "",
          street: address?.street ?? "",
          house_number: address?.house_number ?? "",
          city: address?.city ?? "",
          zip_code: address?.zip_code ?? "",
          country: address?.country ?? "",
          phone_number: address?.phone_number ?? "",
          name: address?.name ?? "",
          surname: address?.surname ?? "",
        };
      }
    }
  }

  return (
    <div className={styles.container}>
      <h1 className={styles.title}>Shipping info</h1>
      
      <form className={styles.form} action={updateAddress}>
        <label className={styles.field}>
          Ulica
          <input
            name="street"
            id="ulica"
            type="text"
            className={styles.input}
            defaultValue={userData.street}
          />
        </label>

        <label className={styles.field}>
          Popisne cislo domu
          <input
            name="house_number"
            type="text"
            id="popisne_cislo_domu"
            className={styles.input}
            defaultValue={userData.house_number}
          />
        </label>

        <label className={styles.field}>
          Mesto
          <input
            name="city"
            type="text"
            id="mesto"
            className={styles.input}
            defaultValue={userData.city}
          />
        </label>

        <label className={styles.field}>
          ZIP code
          <input
            name="zip_code"
            type="text"
            id="zip_code"
            className={styles.input}
            defaultValue={userData.zip_code}
          />
        </label>

        <label className={styles.field}>
          Krajina
          <input
            name="country"
            type="text"
            id="krajina"
            className={styles.input}
            defaultValue={userData.country}
          />
        </label>

        <label className={styles.field}>
          Telefonne cislo
          <input
            name="phone_number"
            type="text"
            id="telefon"
            className={styles.input}
            defaultValue={userData.phone_number}
          />
        </label>

        <label className={styles.field}>
          Email
          <input
            name="email"
            type="email"
            id="email"
            className={styles.input}
            defaultValue={userData.email}
          />
        </label>

        <label className={styles.field}>
          Meno
          <input
            name="name"
            type="text"
            id="meno"
            className={styles.input}
            defaultValue={userData.name}
          />
        </label>

        <label className={styles.field}>
          Priezvisko
          <input
            name="surname"
            type="text"
            id="priezvisko"
            className={styles.input}
            defaultValue={userData.surname}
          />
        </label>

        <div className={styles.buttonContainer}>
          <LinkToConfirmationButton />
        </div>
      </form>
    </div>
  );
}
</file>

<file path="src/app/user/page.tsx">
// user/page.tsx
import getDB from "@/lib/db";
import { cookies } from "next/headers";
import LogoutButton from "./logoutButton";
import Link from "next/link";
import { getFavoriteProducts } from "@/actions/products";
import productStyles from "../products/products.module.css";
import SingleProduct from "../products/singleProduct";

export default async function UserPage() {
    const cookieStore = await cookies();
    const sessionId = cookieStore.get("session")?.value;

    if (!sessionId) return <p className="p-6">Not logged in.</p>;

    const db = getDB();

    const session = await db
        .selectFrom("session")
        .selectAll()
        .where("session_id", "=", sessionId)
        .executeTakeFirst();

    if (!session || session.expiration < Date.now()) {
        return <p className="p-6">Session expired.</p>;
    }

    const user = await db
        .selectFrom("users")
        .selectAll()
        .where("id", "=", session.user_id)
        .executeTakeFirstOrThrow();

    const orders = await db
        .selectFrom("orders")
        .selectAll()
        .where("user_id", "=", user.id)
        .execute();

    const address = await db
        .selectFrom("user_address")
        .selectAll()
        .where("user_id", "=", user.id)
        .executeTakeFirst();
    
    const favoriteProducts = await getFavoriteProducts();

    return (
        <main className="max-w-4xl mx-auto p-6 space-y-8">
            <section className="card">
                <LogoutButton />
                <h1>Account</h1>
                <p><strong>Username:</strong> {user.username}</p>
                <p><strong>Email:</strong> {user.email}</p>

                <div className="flex gap-4 mt-4">
                    <Link href="/password_change" className="btn">Change password</Link>
                    <br />
                    <br />
                    <Link href="/user/email" className="btn">Change email</Link>
                </div>
            </section>

            <section className="card">
                <h2>Address</h2>
                {address ? (
                    <>
                        <p>{address.street} {address.house_number}</p>
                        <p>{address.city}, {address.zip_code}</p>
                        <p>{address.country}</p>
                        <Link href="/user/address" className="btn mt-2">Edit address</Link>
                    </>
                ) : (
                    <Link href="/user/address" className="btn">Add address</Link>
                )}
            </section>

            <section className="card">
                <h2>Orders</h2>
                {orders.length === 0 && <p>No orders yet.</p>}
                {orders.map(order => (
                    <div key={order.id} className="order">
                        <p>Objednavka číslo #{order.id}</p>
                        <p>Status: {order.confirmed ? "Confirmed" : "Pending"}</p>
                    </div>
                ))}
            </section>

            <section className="card">
                <h2>Obľúbené produkty</h2>
                {favoriteProducts.length === 0 ? (
                    <p>Zatiaľ nemáte žiadne obľúbené produkty.</p>
                ) : (
                    <div className={productStyles.grid}>
                        {favoriteProducts.map((product) => (
                            <SingleProduct
                                key={product.id}
                                product={product}
                                userLoggedIn={true}
                            />
                        ))}
                    </div>
                )}
            </section>
        </main>
    );
}
</file>

<file path="src/lib/db-types.ts">
/**
 * This file was generated by kysely-codegen.
 * Please do not edit it manually.
 */

import type { ColumnType } from "kysely";

export type Generated<T> = T extends ColumnType<infer S, infer I, infer U>
  ? ColumnType<S, I | undefined, U>
  : ColumnType<T, T | undefined, T>;

export interface FavoriteProducts {
  id: Generated<number>;
  product_id: number;
  user_id: number;
}

export interface OrderProduct {
  id: Generated<number>;
  order_id: number;
  product_id: number;
  quantity: Generated<number>;
}

export interface Orders {
  city: string;
  confirmed: number;
  country: string;
  email: string;
  house_number: string;
  id: Generated<number>;
  name: string;
  phone_number: string;
  street: string;
  surname: string;
  user_id: number;
  zip_code: string;
}

export interface Products {
  category: string;
  description: string;
  id: Generated<number>;
  image: string;
  name: string;
  price: number;
}

export interface Session {
  expiration: number;
  id: Generated<number>;
  session_id: string;
  user_id: number;
}

export interface UserAddress {
  city: string;
  country: string;
  house_number: string;
  id: Generated<number | null>;
  name: string | null;
  phone_number: string | null;
  street: string;
  surname: string | null;
  user_id: number;
  zip_code: string;
}

export interface Users {
  email: string;
  id: Generated<number>;
  is_admin: Generated<number | null>;
  password: string;
  username: string;
}

export interface DB {
  favorite_products: FavoriteProducts;
  order_product: OrderProduct;
  orders: Orders;
  products: Products;
  session: Session;
  user_address: UserAddress;
  users: Users;
}
</file>

<file path=".gitignore">
node_modules
dokumentacia
.env.local
db.sqlite
.next
</file>

</files>
